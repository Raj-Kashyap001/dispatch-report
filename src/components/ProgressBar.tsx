const ProgressBar = () => (
  <div style={{ margin: "1rem 0" }}>
    <div className="progress-label">Parsing Data...</div>
    <div className="progress-track">
      <div className="progress-bar-fill" />
    </div>
  </div>
);

export default ProgressBar;
